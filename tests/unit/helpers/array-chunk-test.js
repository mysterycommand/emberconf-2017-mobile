
import { arrayChunk } from 'bingo/helpers/array-chunk';
import { module, test } from 'qunit';

module('Unit | Helper | array chunk');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayChunk([42]);
  assert.ok(result);
});

